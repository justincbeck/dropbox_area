if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p136@dropbox_area" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p136@dropbox_area"
else
  rvm --create use  "ruby-1.9.2-p136@dropbox_area"
fi

